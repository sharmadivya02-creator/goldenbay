# Dependencies are reinstalled with `npm install` — never committed
node_modules/

# Secrets live in .env, never in git
.env

# Runtime data (created automatically on first run)
data/db.json

# OS noise
.DS_Store
Thumbs.db
